<?php

namespace Arbitraer\TimeTracking\Tests;

use Arbitraer\TimeTracking\Models\Interval;

class IntervalTest extends TestCase
{
    /** @test */
    public function it_has_a_default_started_at_time()
    {
        // $interval = app(Interval::class)->create();
        // $this->assertTrue(filled($interval->started_at));
        $this->assertTrue(true);
    }
}
